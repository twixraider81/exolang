null function abort();
int function abs( int $n );
//long int abs( long int n );
//extern "C" int atexit( void (*func)(void) );
//extern "C++" int atexit( void (*func)(void) );
//double atof( string $str );
int function atoi( string $str );
//long int atol( string $str );
//long long int atoll( string $str );
//int at_quick_exit( void (*func)(void) );
//void* bsearch( const void* key, const void* base, size_t num, size_t size, int (*compar)(const void*,const void*) );
//void* calloc( size_t num, size_t size );
//div_t div( int numer, int denom );
//ldiv_t div( long int numer, long int denom );
null function exit( int $status );
//void free( void* ptr );
string function getenv( string $name );
//char*  itoa( int value, char * str, int base );
//long int labs( long int n );
//ldiv_t ldiv( long int numer, long int denom );
//long long int llabs( long long int n );
//lldiv_t lldiv( long long int numer, long long int denom );
//void* malloc( size_t size );
//int mblen( const char* pmb, size_t max );
//size_t mbstowcs( wchar_t* dest, const char* src, size_t max );
//int mbtowc( wchar_t* pwc, const char* pmb, size_t max );
//void qsort( void* base, size_t num, size_t size, int (*compar)(const void*,const void*) ) ;
null function quick_exit( int $status );
int function rand();
//void* realloc( void* ptr, size_t size );
//void srand( unsigned int seed );
//double strtod( const char* str, char** endptr );
//float strtof( const char* str, char** endptr );
//long int strtol( const char* str, char** endptr, int base );
//long double strtold( const char* str, char** endptr );
//long long int strtoll( const char* str, char** endptr, int base );
//unsigned long int strtoul( const char* str, char** endptr, int base );
//unsigned long long int strtoull( const char* str, char** endptr, int base );
int function system( string $command );
//size_t wcstombs( char* dest, const wchar_t* src, size_t max );
//int wctomb( char* pmb, wchar_t wc );
null function _Exit( int $status );