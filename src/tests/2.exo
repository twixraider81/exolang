int $a = 1;
int $b = 2;
int $c = 1;

int function puts( string $str );

if( $a < $b ) {
	puts( "$a < $b" );
} else {
	puts( "$a => $b" );
};

if( $a == $c ) {
	puts( "$a == $c" );
};

if( $a == $b ) {
	puts( "$a == $b" );
} else if( $b == $c ) {
	puts( "$b == $c" );
};

return 0;

/*
auto $c = auto $d;
auto $e = 1 + $c;
auto $f = 3 - 2;
auto $g = 3 * $a;
auto $h = ( 4 / 2 );
string $i = '';

// unknown variable usage creates an auto type
$k = 9;

// nested expressions
auto $k = 5 * ( 2 + 2 );
*/