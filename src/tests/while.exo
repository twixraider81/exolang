int function printf( string $str ... );

int $a = 0;
int $b = 5;

while( $a < $b ) {
	$a += 1;
	continue;

	printf( "%i\n", $a );
};
printf( "%i\n", $a );


int $c = 5;
while( true ) {
	$c += 1;
	printf( "%i\n", $c );

	if( $c >= 10 ) {
		break;
	};
};

int $d = 11;
printf( "%i\n", $d );